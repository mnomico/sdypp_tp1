# Los Hits #1 a #7 no requieren dependencias externas: se resuelven con la
# biblioteca estándar de Python 3.11+ (socket, threading, logging, http.server,
# unittest). La configuración se lee de variables de entorno / .env con un
# lector propio (comun/config.py), sin python-dotenv.
#
# Instalación: pip install -r requirements.txt
#
# Dependencias para el Hit #8 (gRPC y Protocol Buffers):
grpcio>=1.68.0
grpcio-tools>=1.68.0
